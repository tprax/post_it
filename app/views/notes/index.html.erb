<h1>Hello Welcome To Post It!</h1>
<h3>post it note app to help you keep track</h3>
<p><%= link_to 'Create New Note', new_note_path %></p>
<% @notes.each do |note| %>
  <h2><%= note.title %></h2>
  <%= link_to 'Show Note', note_path(note.id) %>
<% end %>